// arrays in JavaScript accept any type
var cats = ["tabby", "ginger", "grey"]

// the first argument acts as a label
console.log("Cats", cats);

// set up a new Array like this

var dogs = new Array();

// to add items to an Array in JavaScript

dogs.push("Bulldog");
dogs.push("German Shepherd");

// to find the length in an array in JavaScript

console.log(dogs.length);
